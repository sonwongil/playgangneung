Phase 1-K는 현재 GitHub push까지만 완료되었습니다.

확인된 사항:

1. 커밋 해시: `a338dc2`
2. GitHub push 성공
3. 민감정보 미포함
4. 운영 서버 배포는 아직 미완료

이제 VPS 운영 반영만 남았습니다.

VPS SSH 접속 후 아래 순서로 진행하세요.

---

# STEP 1 — VPS 현재 상태 확인

```bash id="kactkh"
cd /var/www/playgangneung
git status --short -uno
git rev-parse HEAD
pm2 status
```

---

# STEP 2 — 운영 백업

```bash id="68upj1"
ls -ld /var/www/backups 2>/dev/null || mkdir -p /var/www/backups

tar -czf /var/www/backups/playgangneung-before-phase1j-$(date +%Y%m%d-%H%M%S).tar.gz \
  --exclude='/var/www/playgangneung/node_modules' \
  --exclude='/var/www/playgangneung/.pnpm-store' \
  -C /var/www playgangneung

ls -lh /var/www/backups/ | tail -5
```

---

# STEP 3 — 최신 코드 반영

```bash id="fbuijx"
cd /var/www/playgangneung
git fetch origin main
git log --oneline HEAD..origin/main
git pull origin main
git rev-parse HEAD
```

최신 커밋이 `a338dc2`인지 확인하세요.

---

# STEP 4 — build

```bash id="upzq6t"
pnpm --filter @workspace/api-server --filter @workspace/playgangneung-dashboard run build
```

빌드 실패 시 PM2 restart 금지.

---

# STEP 5 — PM2 restart

빌드 성공 시에만 실행하세요.

```bash id="lupzix"
pm2 status
pm2 restart playgangneung
pm2 status
```

확인 기준:

```text id="7agnrk"
playgangneung online
mode fork
instances 1 유지
```

---

# STEP 6 — 운영 확인

브라우저에서 확인:

```text id="fncre2"
https://playgangneung.com/
https://playgangneung.com/home-v2
https://playgangneung.com/home-legacy
https://playgangneung.com/admin
https://playgangneung.com/admin/v2
```

확인할 것:

```text id="u6rerk"
1. / 새 홈 정상
2. 강릉시 소상공인추천 데이터 없을 때 섹션 숨김
3. 강릉노트 빈 상태 문구: 강릉의 새로운 소식을 준비하고 있습니다.
4. /home-legacy 기존 홈 보존
5. /admin, /admin/v2 정상
```

---

# 완료 보고

```text id="u1p4js"
1. VPS 백업 파일명/크기:
2. VPS git pull 결과:
3. VPS 최신 커밋:
4. build 결과:
5. PM2 restart 결과:
6. PM2 mode / instances:
7. / 운영 접속 결과:
8. /home-v2 접속 결과:
9. /home-legacy 접속 결과:
10. /admin, /admin/v2 접속 결과:
11. 강릉시 소상공인추천 확인:
12. 강릉노트 빈 상태 문구 확인:
13. 롤백 필요 여부:
```
