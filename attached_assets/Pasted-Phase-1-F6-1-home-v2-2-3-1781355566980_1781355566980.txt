Phase 1-F6 작업 완료 보고 확인했습니다.

이번 작업은 승인합니다.

잘 지켜진 점:

1. `/home-v2` 안에서만 수정
2. 기존 `/` 홈 유지
3. 기존 `home.tsx` 수정 없음
4. 공식 사업자 푸터 추가
5. 로그인/광고접수/로그아웃 진입점 추가
6. API 수정 없음
7. DB 변경 없음
8. PWA/Service Worker 수정 없음
9. typecheck 통과
10. build 통과
11. GitHub push 없음
12. 운영 서버 수정 없음

좋습니다.

다만 버튼 높이가 `min-h-[36px]`로 보고되었습니다.
앞서 모바일 터치 기준은 44px 이상으로 정했으므로, 가능하면 `min-h-[44px]` 또는 최소 `h-10` 이상으로 재확인해 주세요.

다음 단계는 `/` 교체가 아닙니다.

다음 단계는 **Phase 1-F7: `/home-v2` 메인 교체 전 최종 점검 — 구현 최소화**입니다.

---

# Phase 1-F7 승인 범위

## `/home-v2` 메인 교체 전 최종 점검

이번 단계의 목표는 `/home-v2`를 실제 메인 홈 후보로 교체하기 전에,
핵심 기능이 모두 정상인지 최종 점검하는 것입니다.

이번 단계는 `/` 교체가 아닙니다.
기존 `home.tsx` 수정이 아닙니다.
운영 배포가 아닙니다.

오직 **최종 점검과 필요한 경우 아주 경미한 UI 보정**까지만 진행합니다.

---

## 1. 반드시 점검할 것

다음 항목을 확인하세요.

1. `/home-v2` 직접 접속 정상
2. `/` 기존 홈 영향 없음
3. 오늘의 강릉소식 1~5개 정상 표시
4. 소상공인 추천 카드 정상 표시
5. 강릉노트 피드 정상 표시
6. 검색 정상 동작
7. 카테고리 필터 정상 동작
8. 카드 클릭 시 `/content/:id` 정상 이동
9. 상세 페이지에서 뒤로가기 또는 return_url 정상 동작
10. 로그인 버튼 `/sign-in` 정상 이동
11. 광고접수 버튼 `/ad-submit` 정상 이동
12. 로그인 상태에서 로그아웃 버튼 정상 동작
13. 사업자 푸터 정상 표시
14. 모바일 360px에서 헤더 버튼 깨짐 없음
15. PC 1200px에서 레이아웃 정상
16. 버튼 터치 영역 44px 기준 충족 여부
17. typecheck 통과
18. build 통과

---

## 2. 허용 작업

이번 단계에서 허용하는 작업은 다음뿐입니다.

1. `HomeV2Page.tsx`의 아주 경미한 Tailwind className 보정
2. 로그인/광고접수 버튼 높이 또는 간격 보정
3. 모바일 헤더 깨짐 보정
4. 푸터 줄바꿈 보정
5. typecheck 실행
6. build 실행

---

## 3. 아직 금지

아래 작업은 하지 마세요.

1. `/` 라우트 교체 금지
2. 기존 `home.tsx` 수정 금지
3. 기존 `/` 홈 수정 금지
4. API 수정 금지
5. 새 API 생성 금지
6. DB 변경 금지
7. 마이그레이션 실행 금지
8. PWA 설정 변경 금지
9. Service Worker 수정 금지
10. 광고 신청 폼 수정 금지
11. 결제 기능 연결 금지
12. Meta API 연결 금지
13. SNS 발행 연결 금지
14. GitHub push 금지
15. PM2 restart/reload 금지
16. 운영 서버 수정 금지

---

## 4. 완료 보고 형식

완료 후 아래 형식으로 보고하세요.

1. 수정한 파일 목록
2. `/home-v2` 직접 접속 결과
3. 기존 `/` 홈 영향 여부
4. 오늘의 강릉소식 표시 결과
5. 소상공인 추천 표시 결과
6. 강릉노트 피드 표시 결과
7. 검색/필터 동작 결과
8. 카드 클릭 `/content/:id` 이동 결과
9. 뒤로가기/return_url 확인 결과
10. 로그인 버튼 경로 확인 결과
11. 광고접수 버튼 경로 확인 결과
12. 로그아웃 버튼 확인 결과
13. 사업자 푸터 표시 결과
14. 모바일 360px 점검 결과
15. PC 1200px 점검 결과
16. 버튼 터치 영역 보정 여부
17. typecheck 결과
18. build 결과
19. 금지 사항 위반 여부
20. 최종 판단: `/home-v2`를 `/` 교체 후보로 승인 가능한지

이번 단계는 **메인 교체 전 최종 점검**까지만 승인합니다.
