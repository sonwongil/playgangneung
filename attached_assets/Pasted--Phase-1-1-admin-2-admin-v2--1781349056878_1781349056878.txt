작업 완료 보고 확인했습니다.

Phase 1 최소 골격 구축은 승인합니다.

잘 지켜진 점:

1. 기존 `/admin` 동결 유지
2. `/admin/v2` 병렬 구조 생성
3. 기존 `admin.tsx`, `home.tsx`, `admin-event-detail.tsx` 미수정
4. DB 변경 없음
5. 마이그레이션 없음
6. Meta/결제/PWA 코드 변경 없음
7. typecheck 통과
8. build 통과
9. GitHub push 없음

좋습니다.

다만 다음 단계에서 바로 대시보드 통계부터 구현하지 않습니다.

PLAY강릉 리빌딩의 핵심 흐름은 다음입니다.

강릉소식 수집함
→ 관리자 검토
→ 강릉노트 초안 전환
→ 관리자 수정
→ 오늘의 강릉소식 선정
→ 홈 상단 5개 노출
→ SNS 발행
→ 필요 시 Meta 광고 확장

따라서 다음 작업은 “대시보드 통계”가 아니라, `/admin/v2` 안에서 **강릉소식 수집함의 읽기 전용 목록 화면**을 먼저 구성하는 것입니다.

## 다음 승인 작업: Phase 1-B — 수집함 읽기 전용 목록 화면

이번 단계의 목표는 기능 구현이 아니라, 수집함 화면의 안전한 목록 구조를 만드는 것입니다.

## 허용 작업

1. `/admin/v2/inbox` 화면에 수집된 강릉소식 목록 표시
2. 기존 크롤링/이벤트 API 중 읽기 전용 GET API만 사용
3. TanStack Table 도입이 필요하면 최소 범위로 도입
4. shadcn/ui Table, Badge, Button, Input 조합 사용
5. 제목, 출처, 카테고리, 수집일, 상태, 썸네일, 원본 URL 표시
6. “강릉노트로 전환”, “보류”, “제외”, “광고 후보” 버튼은 아직 동작하지 않는 disabled 또는 placeholder 상태로만 표시
7. typecheck 실행
8. build 실행

## 이번 단계에서 금지

1. DB 변경 금지
2. 마이그레이션 실행 금지
3. POST/PATCH/DELETE 기능 구현 금지
4. “강릉노트로 전환” 실제 동작 구현 금지
5. 보류/제외/광고 후보 상태 변경 구현 금지
6. 기존 `admin.tsx` 수정 금지
7. 기존 `home.tsx` 수정 금지
8. 기존 `admin-event-detail.tsx` 수정 금지
9. Meta API 수정 금지
10. 결제 코드 수정 금지
11. PWA 설정 변경 금지
12. PM2 restart/reload 금지
13. 운영 서버 설정 수정 금지
14. GitHub push 금지

## 데이터 사용 원칙

이번 단계에서는 기존 API를 읽기 전용으로만 사용하세요.

가능하면 기존 `/api/events` 또는 수집함에 해당하는 기존 GET API를 활용하세요.

새 API가 꼭 필요하다고 판단되면, 바로 만들지 말고 먼저 아래를 보고하세요.

1. 왜 기존 GET API로 부족한지
2. 새 API 이름
3. 응답 구조
4. 기존 데이터 영향 여부
5. 보안 영향 여부

## 화면 구성

`/admin/v2/inbox` 화면은 다음 구조로 만드세요.

1. 페이지 제목: 강릉소식 수집함
2. 설명 문구: 크롤링 또는 수동 수집된 강릉소식을 검토해 강릉노트로 전환하는 대기 목록입니다.
3. 검색창
4. 카테고리 필터
5. 상태 필터
6. 목록 테이블

테이블 컬럼:

1. 썸네일
2. 제목
3. 출처
4. 카테고리
5. 수집일
6. 상태
7. 원본 URL
8. 작업 버튼

작업 버튼:

1. 강릉노트로 전환 — placeholder
2. 보류 — placeholder
3. 제외 — placeholder
4. 광고 후보 — placeholder

이번 단계에서는 버튼이 실제 데이터를 변경하면 안 됩니다.

## 완료 보고 형식

완료 후 아래 형식으로 보고하세요.

1. 생성한 신규 파일 목록
2. 수정한 기존 파일 목록
3. 사용한 API
4. 추가 설치한 패키지 여부
5. `/admin/v2/inbox` 접근 결과
6. 표시되는 컬럼 목록
7. 동작하지 않는 placeholder 버튼 목록
8. typecheck 결과
9. build 결과
10. 기존 `/admin` 영향 여부
11. 기존 홈 화면 영향 여부
12. 금지 사항 위반 여부

이번 단계는 수집함의 “읽기 전용 목록 화면”까지만 승인합니다.
