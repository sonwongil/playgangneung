Phase 1-F9 완료 보고 확인했습니다.

이번 작업은 승인합니다.

확인된 상태:

1. `/`에서 새 HomeV2Page 정상 표시
2. `/home-v2`에서 새 HomeV2Page 정상 표시
3. `/home-legacy`에서 기존 Home 정상 보존
4. `/content/:id` 이동 구조 정상
5. `/sign-in`, `/sign-up` 경로 정상
6. `/ad-submit`, `/my-ad` 경로 정상
7. `/admin`, `/admin/v2` 영향 없음
8. 모바일/PC 레이아웃 정상
9. typecheck 통과
10. build 통과
11. 롤백 기준 명확
12. 롤백 방식은 App.tsx 1줄 수정으로 가능

이제 다음 단계로 **GitHub push 전 최종 보안 확인 + 커밋 + push**를 승인합니다.

---

# Phase 1-G 승인 범위

## GitHub push 전 최종 확인 및 커밋

이번 단계의 목표는 지금까지의 리빌딩 작업을 GitHub에 안전하게 기록하는 것입니다.

이번 단계는 운영 서버 배포가 아닙니다.
PM2 재시작이 아닙니다.
nginx, DNS, SSL 수정이 아닙니다.
DB 변경이 아닙니다.

오직 **Git 상태 확인, 보안 확인, 커밋, GitHub push**까지만 진행합니다.

---

## 1. 먼저 확인할 것

커밋 전 아래를 확인하세요.

1. `git status`
2. 변경 파일 목록
3. `.env`, `.env.production`, 비밀키, 토큰 포함 여부
4. `DATABASE_URL` 실제 값 포함 여부
5. Clerk secret, Meta token, Toss secret, Google OAuth secret 포함 여부
6. 사업자등록증 PDF 파일이 git에 포함됐는지 여부
7. 불필요한 스크린샷, 로그 파일, 빌드 산출물 포함 여부
8. `pnpm run typecheck` 결과
9. `pnpm run build` 결과

---

## 2. 특히 주의할 것

아래 파일 또는 값은 절대 커밋하지 마세요.

1. `.env`
2. `.env.production`
3. `.env.local`
4. 실제 `DATABASE_URL`
5. Meta access token
6. Clerk secret key
7. Toss secret key
8. Google OAuth client secret
9. 사업자등록증 PDF 원본
10. 서버 백업 파일
11. DB dump 파일
12. `.tar.gz`, `.sql`, `.bak` 파일
13. 개인정보가 포함된 로그 파일

사업자등록증의 내용은 푸터 텍스트로만 반영하고, PDF 파일 자체는 GitHub에 올리지 마세요.

---

## 3. 허용 작업

다음 작업만 허용합니다.

1. `git status` 확인
2. 변경 파일 목록 보고
3. 보안 민감정보 포함 여부 확인
4. typecheck 실행
5. build 실행
6. 커밋 생성
7. GitHub push

---

## 4. 금지 작업

아래 작업은 하지 마세요.

1. 운영 서버 배포 금지
2. PM2 restart/reload 금지
3. nginx 수정 금지
4. DNS 수정 금지
5. SSL/certbot 수정 금지
6. DB 변경 금지
7. 마이그레이션 실행 금지
8. PWA 설정 변경 금지
9. Service Worker 수동 수정 금지
10. Meta API 수정 금지
11. 결제 코드 수정 금지
12. 기존 `home.tsx` 삭제 금지
13. `/home-legacy` 제거 금지

---

## 5. 커밋 메시지

커밋 메시지는 아래처럼 사용하세요.

```text
feat: rebuild home and admin v2 foundation
```

커밋 설명에는 아래 내용을 포함하세요.

```text
- Add /admin/v2 foundation with inbox, notes, today flow
- Add safe daily_top5 save-only API without changing event status
- Add /home-v2 and promote it to /
- Preserve legacy home at /home-legacy
- Keep existing home.tsx, APIs, DB, PWA settings unchanged
```

---

## 6. push 후 보고 형식

push 완료 후 아래 형식으로 보고하세요.

1. 커밋 해시
2. 브랜치명
3. push 결과
4. 변경 파일 목록 요약
5. 민감정보 포함 여부 확인 결과
6. 사업자등록증 PDF 미포함 여부
7. typecheck 결과
8. build 결과
9. 운영 서버 배포 여부
10. PM2/nginx/DNS/SSL/DB 변경 여부
11. 다음 단계 제안

이번 단계는 **GitHub push까지만** 승인합니다.
운영 서버 배포는 push 완료 보고를 검토한 뒤 별도로 승인합니다.
